# Dart 2.14 Runtime Types

## Installation
To install these types into your IDE, add the following to your `pubspec.yaml` file:
```yaml
dependencies:
  function_types:
    git: 
      url: https://github.com/appwrite/function_types.git
      ref: dart
```
